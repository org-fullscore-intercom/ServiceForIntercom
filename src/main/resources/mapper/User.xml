<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.amazing.intercom.dao.UserDAO">
    <select id="findAll" resultType="User">
        select * from user
    </select>
    <select id="get" parameterType="int" resultType="User">
        select * from user where id=#{id}
    </select>
    <insert id="save"  parameterType="User" >
        insert into user(name,account,password,avatar) values(#{name},#{account},#{password},#{avatar})
    </insert>
    <delete id="delete" parameterType="int">
        delete from user where id=#{id}
    </delete>
    <update id="update" parameterType="User">
        update from user name=#{name},account=#{account},password=#{password},avatar=#{avatar} where id=#{id}
    </update>

</mapper>
